{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace=shared && npm run build:standalone --workspace=client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
